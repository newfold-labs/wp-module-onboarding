/**
 * Constants for query parameters
 */
export const QUERY_PARAMS = {
	REFERRER: 'referrer',
	CANVAS_MODE: 'canvas',
};

/**
 * Valid referrer sources for onboarding integration
 */
export const VALID_REFERRERS = [ 'nfd-onboarding', 'nfd-plugin' ];
